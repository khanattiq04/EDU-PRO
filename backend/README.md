# Danistan Network API

Small Express API contract for the frontend prototype. `POST /api/students` accepts a structured `levelProfile`, centralizes registration number generation, assigns a quiz paper, and encrypts CNIC/B-Form values before storage.
